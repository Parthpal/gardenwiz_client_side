import React from 'react';
import ProfileLoading from '../(user)/profile/Loading';

const LoadingLogin = () => {
    return (
        <div>
            <ProfileLoading/>
        </div>
    );
};

export default LoadingLogin ;